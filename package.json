{
  "name": "stellar-combat",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm run dev -w @stellar/server\" \"npm run dev -w @stellar/client\"",
    "build": "npm run build -w @stellar/client",
    "start": "npm run start -w @stellar/server",
    "test": "npm run test -w @stellar/server",
    "typecheck": "tsc -p shared --noEmit && tsc -p server --noEmit && tsc -p client --noEmit"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
